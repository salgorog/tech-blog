        <article class="post">
            <div class="title">
                <a href="{{post_url}}" target="_blank"> {{title}} </a>
            </div>
            <div class="meta">
                By {{user.username}} on {{created_at}} | <a href="/post/{{id}}">{{comment.length}} comment(s)</a>
            </div>
        </article>